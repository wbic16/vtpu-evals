# AGENTS.md — vtpu-evals

Phext-native LLM evaluation suites. Measures AI literacy in Base 256, phext coordinates, and vTPU architecture.

## Convention
- All lowercase filenames
- JSONL format, OpenAI evals compatible
- Each suite: `<name>/samples.jsonl` + `<name>.yaml`
